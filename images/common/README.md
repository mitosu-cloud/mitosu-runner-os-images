# Common image content

Shared baseline setup, user creation, runner copying, marker generation, and
cleanup logic will live here. Distribution Containerfiles should consume these
files rather than duplicate the guest contract.

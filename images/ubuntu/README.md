# Ubuntu image family

The common stage consumes the digest-pinned Ubuntu 26.04 base and the official
Snapshot Service state in `locks/os/ubuntu-26.04.json`. Profile stages will
extend this Containerfile in Phase 3.
